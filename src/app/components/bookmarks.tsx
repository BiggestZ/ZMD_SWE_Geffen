import { BookEntry } from "./book";

export 


function addBookmark(book : BookEntry) {
    book = 

    // what this should do:
    // make a new csv locally
    // every time the bookmark button is clicked, add the entry to the bookmarks csv
    // when the actual bookmarks page is clicked, all entries can just be generated from the local csv
}