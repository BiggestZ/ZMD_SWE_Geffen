import React from "react";
import { Tag } from "./tag";

const TagsSearch: React.FC = ()  =>  {
    return(
        // search bar (import)
        // tags list
        // dropdowns
    );
};

export default TagsSearch;